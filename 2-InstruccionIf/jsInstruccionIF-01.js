function mostrar()
{
	/* 
	= asiga no compara
	== compra evalua un valor con otro
	si ponemos un if(); es exactamente igual como si 
	no lo tuviera
	false signica 0 en javascrip
	el IF detecta 0
	resultado=edad==15
	if(¿se pregunta esto es 0?)

	function mostrar()
   {
	var edad;

	edad=document.getElementById('txtIdEdad').value;
	edad=parseInt(edad);
	var resultado;

	if(edad=15)
	{

	}
	resultado=edad==15;
	if(edad==15)
	if("fsdfgsdjñgjsd")

*/
	var edad;

	edad=document.getElementById('txtIdEdad').value;
	edad=parseInt(edad);

	if (edad==15) {
		alert("niña bonita");
	}

}//FIN DE LA FUNCIÓN